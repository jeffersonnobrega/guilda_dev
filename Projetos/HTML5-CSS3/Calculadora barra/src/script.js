document.addEventListener("DOMContentLoaded", () => {
    
    const checkbox = document.getElementById("chk")


    checkbox.addEventListener("change", () => {
        let marcado = checkbox.checked
        console.log(marcado)
    })

})

function pegarValor() {
    
}

